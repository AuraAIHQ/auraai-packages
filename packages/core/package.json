{
  "name": "@auraaihq/core",
  "version": "0.0.0",
  "description": "Agent24-Desktop kernel — module loader, IPC, AI Layer adapter framework",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo 'build TBD (M1) — when implemented, use: tsc -p tsconfig.build.json'",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  },
  "publishConfig": {
    "access": "public"
  }
}
