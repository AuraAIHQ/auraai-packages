// @auraaihq/cli — placeholder
// See https://github.com/AuraAIHQ/Agent24-Desktop/blob/main/docs/ROADMAP.md
//
// TODO(M1): wire as actual CLI binary with proper build pipeline
// (compile to dist/, bin: { auraai: "dist/index.js" }, add shebang).
// Right now this is library-style only so tests can import without side effects.

export const VERSION = '0.0.0'

export function main(): void {
  console.log('@auraaihq/cli — M0 placeholder. Real CLI begins M1.')
}
