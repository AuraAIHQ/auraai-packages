{
  "name": "@auraaihq/ai-bridge",
  "version": "0.0.0",
  "private": true,
  "description": "AI Layer router for Agent24 — selects between adapters (idoris/claude/openai/local) based on policy + falls back on failure",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo 'build TBD (M1 next iteration) — when implemented, use: tsc -p tsconfig.build.json'",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "peerDependencies": {
    "@auraaihq/sdk": "workspace:*"
  },
  "devDependencies": {
    "@auraaihq/sdk": "workspace:*",
    "@types/node": "^22.0.0",
    "vitest": "^4.1.5"
  },
  "publishConfig": {
    "access": "public"
  }
}
